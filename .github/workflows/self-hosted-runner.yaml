name: Run on My Local Runner

# This tells GitHub when to trigger the workflow
on:
  push:
    branches:
      - main
  workflow_dispatch:  # allows manual triggering from GitHub UI

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Test Run
        run: echo "Running on my self-hosted runner!"
